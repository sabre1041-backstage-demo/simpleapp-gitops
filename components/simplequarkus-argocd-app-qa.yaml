apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: simplequarkus-qa
  namespace: simpleapp-build
  finalizers:
    - resources-finalizer.argocd.argoproj.io  
spec:
  project: default
  source:
    repoURL: https://github.com/sabre1041-backstage-demo/simplequarkus
    targetRevision: HEAD
    path: .helm/chart
    helm:
      valueFiles:
        - ../value-files/values-qa.yaml
      values: | 
        quarkus-run: 
          image:
            tag: dummy
  destination:
    server: https://kubernetes.default.svc
    namespace: simpleapp-qa
  syncPolicy:
    automated: 
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=false
    retry:
      backoff:
        duration: 5s # the amount to back off. Default unit is seconds, but could also be a duration (e.g. "2m", "1h")
        factor: 2 # a factor to multiply the base duration after each failed retry
        maxDuration: 10m # the maximum amount of time allowed for the backoff strategy
  ignoreDifferences:      
  - group: networking.istio.io
    kind: VirtualService
    name: simplequarkus
    namespace: simpleapp-qa
    jsonPointers:
    - /spec/hosts                          